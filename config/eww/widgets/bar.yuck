(defwidget bar []
  (centerbox :orientation "h"
    (box 
      :class "island left" 
      :halign "start" 
      :orientation "h"
      :space-evenly false
      :spacing 20

      (cpu_gpu)
      (memory)
    )

    (box
      :class "island middle"
      :halign "center"

      (workspaces)
    )

    (box
      :class "island right"
      :halign "end"
      :orientation "h"
      :space-evenly false
      :spacing 20

      (now_playing)
      (mullvad)
      (clock)
    )
  )
)
