(defwidget mullvad []
  (button
    :onclick "/home/blousy/.dotfiles/config/eww/scripts/toggle-mullvad.sh"
    :class "mullvad-widget ${mullvad_status}" 
    (box :orientation "h"
      (label :text "󰳌" :visible "${mullvad_status == "connected"}")
      (label :text "󰦜" :visible "${mullvad_status == "disconnected"}")
      (label :text "󰻍" :visible "${mullvad_status == "connecting"}")
    )
  )
)
